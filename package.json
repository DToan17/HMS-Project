{
  "name": "hospital-dashboard",
  "version": "1.0.0",
  "description": "Hospital Management Dashboard - Full Stack",
  "scripts": {
    "start": "node scripts/start.js",
    "dev": "concurrently --kill-others --names \"API,UI\" --prefix-colors \"cyan,magenta\" \"npm start --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "build": "npm install --prefix frontend && npm run build --prefix frontend"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
